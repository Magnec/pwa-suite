{
  "name": "drupal/pwa_suite",
  "description": "Comprehensive PWA support for Drupal: Manifest, Service Worker, Push Notifications.",
  "type": "drupal-module",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.1",
    "minishlink/web-push": "^9.0 || ^10.0"
  },
  "suggest": {
    "ext-gmp": "Required for VAPID key signing (recommended for performance)",
    "ext-bcmath": "Alternative to gmp for VAPID key signing",
    "ext-sodium": "Recommended for push payload encryption"
  }
}
